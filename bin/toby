#!/usr/bin/env bash
# The `toby` command. Installed as ~/.local/bin/toby by install.sh.
HERE="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"
PY="$HERE/.venv/bin/python"
[ -x "$PY" ] || PY="$(command -v python3)"
exec "$PY" "$HERE/scripts/toby_cli.py" "$@"
